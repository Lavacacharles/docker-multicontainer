 version: "3.3"

 services:
   api-students:
     build: ./api-students
     image: chichovelo/api-students
     ports:
       - 8000:8000
   websimple:
     build: ./websimple
     image: chichovelo/websimple
     ports:
       - 8080:80
